// Copyright 2024 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/// Material 3 color tokens for the Sunflower app.
///
/// These values are ported directly from the Android Sunflower
/// `ui/Color.kt` file to ensure pixel-perfect parity between the
/// Android Compose and Flutter implementations.
library;

import 'package:flutter/material.dart';

// ---------------------------------------------------------------------------
// Light theme colors
// ---------------------------------------------------------------------------

/// Primary green — light theme.
const Color mdThemeLightPrimary = Color(0xFF246D00);

/// On-primary — light theme.
const Color mdThemeLightOnPrimary = Color(0xFFFFFFFF);

/// Primary container — light theme.
const Color mdThemeLightPrimaryContainer = Color(0xFFA6F780);

/// On-primary container — light theme.
const Color mdThemeLightOnPrimaryContainer = Color(0xFF062100);

/// Secondary — light theme.
const Color mdThemeLightSecondary = Color(0xFF55624C);

/// On-secondary — light theme.
const Color mdThemeLightOnSecondary = Color(0xFFFFFFFF);

/// Secondary container — light theme.
const Color mdThemeLightSecondaryContainer = Color(0xFFD8E7CB);

/// On-secondary container — light theme.
const Color mdThemeLightOnSecondaryContainer = Color(0xFF131F0D);

/// Tertiary — light theme.
const Color mdThemeLightTertiary = Color(0xFF386667);

/// On-tertiary — light theme.
const Color mdThemeLightOnTertiary = Color(0xFFFFFFFF);

/// Tertiary container — light theme.
const Color mdThemeLightTertiaryContainer = Color(0xFFBBEBEC);

/// On-tertiary container — light theme.
const Color mdThemeLightOnTertiaryContainer = Color(0xFF002021);

/// Error — light theme.
const Color mdThemeLightError = Color(0xFFBA1A1A);

/// Error container — light theme.
const Color mdThemeLightErrorContainer = Color(0xFFFFDAD6);

/// On-error — light theme.
const Color mdThemeLightOnError = Color(0xFFFFFFFF);

/// On-error container — light theme.
const Color mdThemeLightOnErrorContainer = Color(0xFF410002);

/// Background — light theme.
const Color mdThemeLightBackground = Color(0xFFFDFDF6);

/// On-background — light theme.
const Color mdThemeLightOnBackground = Color(0xFF1A1C18);

/// Surface — light theme.
const Color mdThemeLightSurface = Color(0xFFFDFDF6);

/// On-surface — light theme.
const Color mdThemeLightOnSurface = Color(0xFF1A1C18);

/// Surface variant — light theme.
const Color mdThemeLightSurfaceVariant = Color(0xFFDFE4D7);

/// On-surface variant — light theme.
const Color mdThemeLightOnSurfaceVariant = Color(0xFF43483E);

/// Outline — light theme.
const Color mdThemeLightOutline = Color(0xFF73796D);

/// Inverse on-surface — light theme.
const Color mdThemeLightInverseOnSurface = Color(0xFFF1F1EA);

/// Inverse surface — light theme.
const Color mdThemeLightInverseSurface = Color(0xFF2F312D);

/// Inverse primary — light theme.
const Color mdThemeLightInversePrimary = Color(0xFF8BDA67);

/// Shadow — light theme.
const Color mdThemeLightShadow = Color(0xFF000000);

/// Surface tint — light theme.
const Color mdThemeLightSurfaceTint = Color(0xFF246D00);

/// Outline variant — light theme.
const Color mdThemeLightOutlineVariant = Color(0xFFC3C8BB);

/// Scrim — light theme.
const Color mdThemeLightScrim = Color(0xFF000000);

// ---------------------------------------------------------------------------
// Dark theme colors
// ---------------------------------------------------------------------------

/// Primary green — dark theme.
const Color mdThemeDarkPrimary = Color(0xFF8BDA67);

/// On-primary — dark theme.
const Color mdThemeDarkOnPrimary = Color(0xFF0F3900);

/// Primary container — dark theme.
const Color mdThemeDarkPrimaryContainer = Color(0xFF195200);

/// On-primary container — dark theme.
const Color mdThemeDarkOnPrimaryContainer = Color(0xFFA6F780);

/// Secondary — dark theme.
const Color mdThemeDarkSecondary = Color(0xFFBCCBB0);

/// On-secondary — dark theme.
const Color mdThemeDarkOnSecondary = Color(0xFF273421);

/// Secondary container — dark theme.
const Color mdThemeDarkSecondaryContainer = Color(0xFF3E4A36);

/// On-secondary container — dark theme.
const Color mdThemeDarkOnSecondaryContainer = Color(0xFFD8E7CB);

/// Tertiary — dark theme.
const Color mdThemeDarkTertiary = Color(0xFFA0CFD0);

/// On-tertiary — dark theme.
const Color mdThemeDarkOnTertiary = Color(0xFF003738);

/// Tertiary container — dark theme.
const Color mdThemeDarkTertiaryContainer = Color(0xFF1E4E4F);

/// On-tertiary container — dark theme.
const Color mdThemeDarkOnTertiaryContainer = Color(0xFFBBEBEC);

/// Error — dark theme.
const Color mdThemeDarkError = Color(0xFFFFB4AB);

/// Error container — dark theme.
const Color mdThemeDarkErrorContainer = Color(0xFF93000A);

/// On-error — dark theme.
const Color mdThemeDarkOnError = Color(0xFF690005);

/// On-error container — dark theme.
const Color mdThemeDarkOnErrorContainer = Color(0xFFFFDAD6);

/// Background — dark theme.
const Color mdThemeDarkBackground = Color(0xFF1A1C18);

/// On-background — dark theme.
const Color mdThemeDarkOnBackground = Color(0xFFE3E3DC);

/// Surface — dark theme.
const Color mdThemeDarkSurface = Color(0xFF1A1C18);

/// On-surface — dark theme.
const Color mdThemeDarkOnSurface = Color(0xFFE3E3DC);

/// Surface variant — dark theme.
const Color mdThemeDarkSurfaceVariant = Color(0xFF43483E);

/// On-surface variant — dark theme.
const Color mdThemeDarkOnSurfaceVariant = Color(0xFFC3C8BB);

/// Outline — dark theme.
const Color mdThemeDarkOutline = Color(0xFF8D9287);

/// Inverse on-surface — dark theme.
const Color mdThemeDarkInverseOnSurface = Color(0xFF1A1C18);

/// Inverse surface — dark theme.
const Color mdThemeDarkInverseSurface = Color(0xFFE3E3DC);

/// Inverse primary — dark theme.
const Color mdThemeDarkInversePrimary = Color(0xFF246D00);

/// Shadow — dark theme.
const Color mdThemeDarkShadow = Color(0xFF000000);

/// Surface tint — dark theme.
const Color mdThemeDarkSurfaceTint = Color(0xFF8BDA67);

/// Outline variant — dark theme.
const Color mdThemeDarkOutlineVariant = Color(0xFF43483E);

/// Scrim — dark theme.
const Color mdThemeDarkScrim = Color(0xFF000000);

// ---------------------------------------------------------------------------
// Seed color
// ---------------------------------------------------------------------------

/// The seed color used to generate the Material 3 color scheme.
const Color seedColor = Color(0xFF256F00);
